# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET     /generate                   controllers.Application.generate
GET     /up/*variant                         controllers.Application.up(variant)
GET     /down/*variant                       controllers.Application.down(variant)

# Static resources
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
GET     /webjars/*file              controllers.WebJarAssets.at(file)
GET     /audio/*file                controllers.Application.getAudioFile(file)